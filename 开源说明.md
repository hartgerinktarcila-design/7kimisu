# 开源说明 — 7kimisu v2.27

本文件是这份源码包的说明。源码包由本仓库 **v2.27** 标签（提交 `bd07e4a`）导出，与同版本发布的
`7kimisu-v2.27.apk` 一一对应。

---

## 一、这是什么

**7kimisu 是 [KernelSU](https://github.com/tiann/KernelSU) 的第三方修改版（非官方分支）**，
在上游基础上二次开发，改动集中在 Android 管理器（界面、个性化、特效等）与少量内核侧行为。

- 对外发布的是编译好的 APK；按上游所采用的开源协议要求，这里提供**与该 APK 对应的完整源码**，
  任何人都可以查看、修改、自行编译、再分发。
- 主要改动（详见源码注释与本仓库 README）：
  - 管理器：个性化（壁纸 / 状态卡 / 扁平化 / 透明度）、隐身模式、下雪与巨魔雨特效、
    字体与字重、主题调色板、导航图标、开屏公告等；
  - 内核侧：隐身模式下的 `GET_INFO` 行为、管理器签名信任集（编译期写死，只信任本管理器）；
  - v2.27 本版：**删掉自创的模块管理机制，退回上游 KernelSU 的写法**（升级 / 安装 / 卸载 /
    批量标记一律照上游实现）。

## 二、许可证（License）

本项目是 **GPL 家族的自由软件**，许可方式与上游 KernelSU 保持一致，**不附加任何额外限制**：

| 范围 | 许可证 | 原文位置 |
|---|---|---|
| `kernel/` 及其子目录 | **GPL-2.0-only** | `kernel/LICENSE` |
| 其余全部（`manager/` `userspace/` `uapi/` `js/` 等） | **GPL-3.0-or-later** | 根目录 `LICENSE` |

- 根目录 `LICENSE` 是 **GNU 通用公共许可证第 3 版（GPL-3.0）** 的完整原文。
- 许可证允许你自由使用、修改、再分发本软件（包括用于商业目的），但必须：
  1. **保留版权声明与许可证原文**；
  2. **分发二进制（例如 APK）时，一并提供对应的完整源码**；
  3. 对你修改过的部分，同样以 GPL 授权（copyleft）。
- 本项目自己的修改部分，也以上述许可证授权给所有人。

## 三、出处与致谢

本项目基于 [**KernelSU**](https://github.com/tiann/KernelSU) 二次开发。
感谢作者 **weishu(tiann)** 与所有社区贡献者把项目开源 —— 没有上游，就没有 7kimisu。

上游项目同样是 GPL 作品，本仓库的目录级许可划分（kernel 与其余部分）与上游保持一致。

## 四、怎么编译

### 4.0 环境要求（与仓库实际配置一致）

| 组件 | 要求 |
|---|---|
| JDK | **21**（CI 用 Temurin 21；`manager/build.gradle.kts` 里 source/target 均为 Java 21） |
| Gradle | **9.7.1**（用仓库自带的 `./gradlew`，会自动下载） |
| Android Gradle Plugin | **9.4.0** |
| Kotlin | **2.4.10** |
| Android SDK | compileSdk **37**、buildTools **37.0.0**、minSdk **31**、targetSdk **37** |
| Android NDK | **29.0.14206865**（r29） |
| Rust | stable（工作区 `edition = "2024"` → 需要 **Rust 1.85 或更新**；CI 用 `rustup update stable`） |
| Python | 3（跑 `repack_apk.py`） |
| CMake | ≥ 3.18.1（编 `libsevenk.so` / `libadbroot.so`） |
| 内核模块 | 目标机型的内核源码或 DDK；CI 用镜像 `ghcr.io/ylarod/ddk-min:<KMI>-20260828` |

支持的 KMI（内核模块按这个维度出多个文件）：
`android12-5.10`、`android13-5.10`、`android13-5.15`、`android14-5.15`、`android14-6.1`、
`android15-6.6`、`android16-6.12`、`android17-6.18`。

> **建议**：把源码包解开后先 `git init && git add -A && git commit -m init`。
> 管理器的版本号逻辑会调用 `git describe`，在有 git 仓库的目录里构建最省事（见 4.3 的替代办法）。

### 4.1 编译内核模块（`kernelsu.ko`）

```bash
cd kernel
CONFIG_KSU=m CC=clang make            # 单 KMI 通用写法（完整矩阵见 .github/workflows-disabled/ddk-lkm.yml）
```

⚠️ **签名信任集（重要）**：`kernel/Kbuild` 里默认写死了本发布版 APK 的**签名证书指纹**
（`KSU_EXPECTED_SIZE` / `KSU_EXPECTED_HASH`，是证书公钥摘要，不是私钥）。你自己编译的管理器
APK 用的是你自己的签名，内核默认**不会信任**它。请改成自己的证书：

```bash
# 1) 从你自己的签名库导出证书，算出“大小(十六进制)”与 sha256
keytool -exportcert -keystore your.jks -alias your-alias -file cert.der
printf '0x%04x' "$(stat -f%z cert.der)"          # macOS；Linux 用 stat -c%s
shasum -a 256 cert.der                            # Linux 用 sha256sum

# 2) 编内核模块时覆盖（Kbuild 里是 ifndef，命令行可直接覆盖）
CONFIG_KSU=m CC=clang make \
  KSU_EXPECTED_SIZE=<第 1 步算出的 0x....> \
  KSU_EXPECTED_HASH=<第 1 步算出的 sha256>
```

产物为 `kernelsu.ko`，按 KMI 命名（例如 `android15-6.6_sevenk.ko`）。

### 4.2 编译 `ksuinit` 与 `ksud`（最终会成为 APK 里的 `libksud.so`）

```bash
# 准备 Rust 交叉编译环境（NDK r29）
python3 scripts/setup_cargo_config.py --ndk-root "$ANDROID_NDK_HOME"   # 生成 .cargo/config.toml
rustup target add aarch64-linux-android x86_64-linux-android

# a) 编 ksuinit（注意 RUSTFLAGS：原 CI 里是这样编的，见 .github/workflows-disabled/ksuinit.yml）
source .github/scripts/setup-rust-build.sh aarch64-linux-android 26
BUILTINS="$($CLANG_PATH --print-resource-dir)/lib/linux/libclang_rt.builtins-aarch64-android.a"
export RUSTFLAGS="-C target-feature=+crt-static -C link-arg=-Wl,-z,max-page-size=16384 \
  -C link-arg=-Wno-unused-command-line-argument -C link-arg=$BUILTINS"
cargo build --package ksuinit --target=aarch64-linux-android --release

# b) 把内核模块与 ksuinit 放进 ksud 的嵌入目录（ksud 会把它们嵌进自己身上）
cp android15-6.6_sevenk.ko                      userspace/ksud/bin/aarch64/
cp target/aarch64-linux-android/release/ksuinit userspace/ksud/bin/aarch64/

# c) 编 ksud
cargo build --manifest-path userspace/ksud/Cargo.toml --release --target aarch64-linux-android
```

x86_64 同理（`userspace/ksud/bin/x86_64/`）。产物在 `target/<target>/release/ksud`。

> 说明：`userspace/ksud/bin/*/busybox` 已在仓库里；`*.ko` 与 `ksuinit` 属于构建产物，仓库不含，
> 需要按 4.1 自己编（`.gitignore` 已把它们排除）。

### 4.3 编译管理器 APK

```bash
cd manager
./gradlew :app:assembleRelease \
  -PKSU_NAME=7kimisu \
  -PKSU_VERSION_NAME=v2.27 \
  -PKEYSTORE_FILE=<你的签名库路径> \
  -PKEYSTORE_PASSWORD=<你的签名库口令> \
  -PKEY_ALIAS=<你的别名> \
  -PKEY_PASSWORD=<你的私钥口令>
```

- 产物：`manager/app/build/outputs/apk/release/*.apk`（这一步**还没有** ksud，见 4.4）。
- 在当前目录**没有 git 仓库**时，`-PKSU_VERSION_NAME=` 必须传（否则 Gradle 会去跑 `git describe` 而报错）。
- **不要传** `-PKSU_VERSION_CODE=`：该参数只在“大于按版本名推导出的值”时才生效，否则被忽略并提示；
  versionCode 由版本名推导（`v2.27` → `2270000`），写死会造成“新版装不上”。
- 想要非 release 的调试包可以用 `./gradlew :app:assembleDebug`。

### 4.4 把 ksud 打进 APK 并重新签名（项目发布用的方式）

```bash
mkdir -p target/aarch64-linux-android/release
cp <上一步编好的 ksud> target/aarch64-linux-android/release/ksud

python3 repack_apk.py repack -b release -t release \
  -a arm64-v8a -a x86_64 --strip \
  -K <你的签名库路径> -A <别名> \
  -P <你的签名库口令> -S <你的私钥口令>
```

`repack_apk.py` 负责：把 `target/<target>/release/ksud` 作为 `lib/<abi>/libksud.so` 塞进 APK、
按需 strip、重新签名，产物在 `dist/`。这就是 APK 里 `libksud.so` 的来源
（`libsevenk.so`、`libadbroot.so` 则由 CMake 在 4.3 里编出）。

> 仓库里的 `-PPREBUILT_NATIVE=1`（README 旧写法）在当前代码里已无引用，传了也不会生效。

## 五、怎么用（安装大方向）

管理器只是“前端”，root 能力来自内核里的 KernelSU 模块，所以**先要有支持的内核**。

1. **前提**：设备已解锁 Bootloader；管理器要求 Android 12+（minSdk 31）；GKI 设备最省事。
2. **装管理器**：安装 `7kimisu-v2.27.apk`（arm64-v8a / x86_64）。
3. **装内核（LKM 方式，推荐 GKI 设备）**：打开管理器 → 按界面提示“安装 / 修补”，
   它会按设备 KMI 选对应的 `kernelsu.ko` 打进 `init_boot`（或 `boot`）镜像；
4. **刷入**：把修补后的镜像用 `fastboot flash init_boot <修补后的镜像>` 刷回，重启。
5. **非 GKI / 自定义内核**：需要自己编译内核并集成（`kernel/` 目录，`CONFIG_KSU=m`），
   或用 DDK 编出的 `.ko`；集成细节见 `kernel/` 与上游 KernelSU 文档。
6. 已经在官方 KernelSU 内核上的设备，本管理器支持“双向换 root”，可以在不重新刷机的前提下换装。

⚠️ 刷机有风险：**先备份**（尤其是 `init_boot` / `boot` 原始镜像），出问题自负后果。
别拿它去做坏事；看到倒卖的，顺手举报一下就好。

## 六、不提供什么（请务必知悉）

- **不提供签名密钥**：仓库、源码包与文档里都没有 `.jks` / `.p12` / `.keystore` 私钥或口令；
  自己编译请自备签名库。
- **不提供任何人的个人数据**：源码包内不含用户数据、账号、令牌。
- **不提供预编译二进制**：本包不含 `*.apk`、`*.so`、`*.ko` 等构建产物（那是编译出来的）。
- **不承诺适配所有机型**：能否使用取决于你的内核版本 / KMI 与设备分区结构；非 GKI 设备通常需要
  自己编译内核。
- 不保证与上游 KernelSU 的模块、管理器、内核互相兼容——这是一份修改版，行为与上游可能有差异。

## 七、免费

本项目**完全免费**：没有付费版、没有会员、没有卡密、没有激活码，也不卖任何东西。
**如果你是花钱买来的，那你就是被骗了** —— 请找卖家退款、举报。

## 八、关于这份源码包

- 由 git 标签 `v2.27`（提交 `bd07e4a`）导出，内容与标签一致，**除了**：
  - **未包含**与构建无关的内部项目文档（项目内部协作文档、AI 规则文件等）；
  - **未包含** `website/`（那是上游 KernelSU 的官方文档站快照，本仓库未做改动，也不参与本项目构建；
    需要请到上游仓库获取）；
  - **少量注释/文档脱敏**：删除了指向项目内部文档的引用与一个内部下载链接（不涉及代码逻辑，
    编译产物不受影响）。
- 因此本包 **开箱即可按上面第 4 章编译**；若你需要“逐字节等同标签”的版本，可直接从 git 仓库的
  `v2.27` 标签导出。
